# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
npm-debug.log*
package-lock.json

# Build artifacts
styles.min.css
*.map
# Login console ships its own pre-built Tailwind output (Cloudflare Pages has no build step yet).
!login/styles.min.css

# Temporary files
"temporary screenshots"/
*.tmp

# WordPress migration artifacts (not part of the static site)
*.zip
nexus-blog-posts.xml
nexus-redirects.csv
elementor-homepage.html
homepage-rates-snippet.html
rates-widget-wordpress.html
rates-widget.html
WordPress\ Migration\ Pack.md

# Backup folders
Backup/
blog/blog-images-png-backup/

# Local dev scratch files
index-old.html
blog/index-old.html
zi2FQSBP

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
.claude/

# SEO audit work products (local-only)
seo-audit-screenshots/
seo-audit-screenshots-v2/
ACTION-PLAN.md
FULL-AUDIT-REPORT*.md
VALIDATION-REPORT.md
capture_nexus.py

# Internal handoff docs (do not publish)
GBP-handoff.md
GBP-listing-copy.md

# Secrets — NEVER commit. Use GitHub Action secrets in production.
.env
.env.*
!.env.example
*.pem
*.key

# Google Drive sync conflict duplicates (e.g. "CNAME 2", "favicon 2.ico")
* 2
* 2.*

# Unused image-tool drop folders (e.g. "crossport" infographic exports) — not referenced by any page
**/crossport/

# Private / internal — not for the public static site
login/rates.xlsx
login/*.xlsx
GBP-photos-todo.md
RATES-UPDATE-GUIDE.md
blog/blog-images/placeholder.txt

# Private rate source workbooks — NEVER commit (advisor pricing + BOC contact list).
# CI decrypts rates.xlsx.enc via the RATES_XLSX_KEY secret to rebuild rates.json.
/rates.xlsx
/Rates/

# Ads audit work products (local-only, do not deploy)
ADS-AUDIT-REPORT.md
ADS-ACTION-PLAN.md
ADS-QUICK-WINS.md
